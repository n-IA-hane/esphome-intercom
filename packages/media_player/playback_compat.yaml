# Shared playback-completion and Sendspin compatibility adapters.
# Keeps playback accounting and pending-source cancellation consistent.
esphome:
  min_version: 2026.9.0

external_components:
  - source: ${ext_components_source}
    components: [mixer, speaker_source]
